version: '3.9'

services:

  postgres:
    image: postgres:16
    container_name: fintax-postgres
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5444:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
